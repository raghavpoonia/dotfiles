# .zprofile
# raghavpoonia/dotfiles
# Managed by chezmoi
# Loaded once at login (not on every shell start)

# ── Homebrew ──────────────────────────────────────────────────────────────────
# ARM vs Intel path handled by chezmoi template
{{ template "brew_path.tmpl" . }}
